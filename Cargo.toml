[package]
name = "georgikon"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18.1"
bevy_third_person_camera = "0.4.0"
rusqlite = { version = "0.39", features = ["bundled"] }

# Compile dependencies with optimizations in debug builds.
# Without this Bevy runs at ~5 fps in dev; with it, near release speed.
[profile.dev.package."*"]
opt-level = 3
